name: zava_medallion_dbt123
version: 1.0
config-version: 2

profile: zava_profile

model-paths: ["models"]

models:
  zava_medallion_dbt123:
    silver:
      materialized: table
      tags: ["silver"]
    gold:
      materialized: table
      tags: ["gold"]
